# MB_selector3000
xgboost implementation for MB protein selection from list of proteins

papildomai:
prideti bioconda chanell:
conda config --add channels defaults
conda config --add channels bioconda
conda config --add channels conda-forge

irasyti bioservises
irasyti xlrd
